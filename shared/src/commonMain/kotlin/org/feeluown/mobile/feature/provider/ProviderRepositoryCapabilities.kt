package org.feeluown.mobile

/**
 * Migration marker for the P4 architecture fitness check.
 *
 * Provider capability contracts live in :provider:api. The former ProviderMusicRepository
 * aggregate and its compatibility view adapters are intentionally absent.
 */
