name: Nucleus Desktop Packaging

"on":
  workflow_call:

permissions:
  contents: read

jobs:
  windows-x64:
    name: Windows x64 MSI
    runs-on: windows-latest
    timeout-minutes: 75
    env:
      FUOEVOLVE_NUCLEUS_LIBMPV_DEV_DIR: ${{ github.workspace }}\build\nucleus-native\windows\mpv
      FUOEVOLVE_NUCLEUS_LIBMPV_RUNTIME_DIR: ${{ github.workspace }}\build\nucleus-native\windows\mpv

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Prepare Git refs for versioning
        shell: pwsh
        run: |
          git show-ref --verify --quiet refs/heads/master
          if ($LASTEXITCODE -ne 0) {
            git branch master origin/master
          }

      - name: Set up Nucleus GraalVM toolchain
        uses: NucleusFramework/Nucleus/.github/actions/setup-nucleus@v2.5.15
        with:
          java-version: "17"
          graalvm: "true"
          packaging-tools: "false"
          setup-node: "true"

      - name: Set up Rust 1.98.1
        uses: dtolnay/rust-toolchain@1.98.1

      - name: Cache Rust WebView helper
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            desktopApp/native/web-login/target
          key: nucleus-web-login-windows-x64-rust-1.98.1-${{ hashFiles('desktopApp/native/web-login/Cargo.toml', 'desktopApp/native/web-login/Cargo.lock', 'desktopApp/native/web-login/src/**') }}
          restore-keys: |
            nucleus-web-login-windows-x64-rust-1.98.1-

      - name: Cache pinned Windows libmpv bundle
        id: windows-libmpv-cache
        uses: actions/cache@v6
        with:
          path: build/nucleus-native/windows/mpv
          key: nucleus-libmpv-windows-x64-${{ hashFiles('desktopApp/packaging/native-deps.lock') }}

      - name: Prepare pinned Windows libmpv development bundle
        if: steps.windows-libmpv-cache.outputs.cache-hit != 'true'
        shell: pwsh
        run: >-
          ./desktopNucleusPoc/packaging/windows/prepare-libmpv-dev.ps1
          -OutputDir "$env:FUOEVOLVE_NUCLEUS_LIBMPV_DEV_DIR"

      - name: Build Nucleus Native Image MSI
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          clang --version
          lld-link --version
          .\gradlew.bat `
            --no-daemon `
            --no-configuration-cache `
            --warning-mode all `
            --stacktrace `
            -PenableNucleusDesktopPoc=true `
            -PnativeMarch=compatibility `
            -Pfuoevolve.nucleus.targetFormat=msi `
            :desktopNucleusPoc:packageGraalvmMsi
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Prepare Windows artifact
        shell: pwsh
        run: |
          $artifacts = Get-ChildItem -Path desktopNucleusPoc\build -Recurse -File -Filter *.msi
          if ($artifacts.Count -ne 1) {
            Write-Host "Expected exactly one MSI, found $($artifacts.Count)"
            $artifacts | Select-Object FullName, Length
            exit 1
          }
          New-Item -ItemType Directory -Force -Path build\artifacts | Out-Null
          Copy-Item $artifacts[0].FullName build\artifacts\FuoEvolve-windows-x64.msi
          Get-Item build\artifacts\FuoEvolve-windows-x64.msi | Select-Object Name, Length

      - name: Upload Windows MSI
        uses: actions/upload-artifact@v7
        with:
          name: FuoEvolve-windows-x64-msi
          path: build/artifacts/FuoEvolve-windows-x64.msi
          archive: false
          retention-days: 14
          if-no-files-found: error

  macos:
    name: macOS ${{ matrix.arch }} DMG
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            runner: macos-15
            uname_arch: arm64
          - arch: x64
            runner: macos-15-intel
            uname_arch: x86_64

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Verify runner architecture
        run: test "$(uname -m)" = "${{ matrix.uname_arch }}"

      - name: Prepare Git refs for versioning
        run: |
          if ! git show-ref --verify --quiet refs/heads/master; then
            git branch master origin/master
          fi

      - name: Set up Nucleus GraalVM toolchain
        uses: NucleusFramework/Nucleus/.github/actions/setup-nucleus@v2.5.15
        with:
          java-version: "17"
          graalvm: "true"
          packaging-tools: "false"
          setup-node: "true"

      - name: Set up Rust 1.98.1
        uses: dtolnay/rust-toolchain@1.98.1

      - name: Cache Rust WebView helper
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            desktopApp/native/web-login/target
          key: nucleus-web-login-macos-${{ matrix.arch }}-rust-1.98.1-${{ hashFiles('desktopApp/native/web-login/Cargo.toml', 'desktopApp/native/web-login/Cargo.lock', 'desktopApp/native/web-login/src/**') }}
          restore-keys: |
            nucleus-web-login-macos-${{ matrix.arch }}-rust-1.98.1-

      - name: Prepare relocatable macOS libmpv runtime
        run: |
          bash desktopApp/packaging/macos/prepare-libmpv.sh \
            "${{ github.workspace }}/build/nucleus-native/macos/mpv"
          MPV_PREFIX="$(brew --prefix mpv)"
          echo "FUOEVOLVE_NUCLEUS_LIBMPV_DEV_DIR=$MPV_PREFIX" >> "$GITHUB_ENV"
          echo "FUOEVOLVE_NUCLEUS_LIBMPV_RUNTIME_DIR=${{ github.workspace }}/build/nucleus-native/macos/mpv" >> "$GITHUB_ENV"

      - name: Build Nucleus Native Image DMG
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          ./gradlew \
            --no-daemon \
            --no-configuration-cache \
            --warning-mode all \
            --stacktrace \
            -PenableNucleusDesktopPoc=true \
            -PnativeMarch=compatibility \
            -Pfuoevolve.nucleus.targetFormat=dmg \
            :desktopNucleusPoc:packageGraalvmDmg

      - name: Verify macOS package closure
        run: |
          JNI="$(find desktopNucleusPoc/build/compose/binaries -type f -name 'libfuoevolve_mpv_jni.dylib' -print -quit)"
          MPV="$(find desktopNucleusPoc/build/compose/binaries -type f -name 'libmpv.dylib' -print -quit)"
          HELPER="$(find desktopNucleusPoc/build/compose/binaries -type f -name 'fuoevolve-web-login' -print -quit)"
          test -n "$JNI" && test -n "$MPV" && test -n "$HELPER"
          if otool -L "$JNI" "$MPV" | grep -E '/(opt/homebrew|usr/local)/(Cellar|opt)/'; then
            echo "Nucleus macOS package still references Homebrew" >&2
            exit 1
          fi

      - name: Prepare macOS artifact
        run: |
          mapfile -t artifacts < <(find desktopNucleusPoc/build -type f -name '*.dmg' -print)
          if [[ "${#artifacts[@]}" -ne 1 ]]; then
            echo "Expected exactly one DMG, found ${#artifacts[@]}" >&2
            printf '%s\n' "${artifacts[@]}" >&2
            exit 1
          fi
          mkdir -p build/artifacts
          cp "${artifacts[0]}" "build/artifacts/FuoEvolve-macos-${{ matrix.arch }}.dmg"
          du -h "build/artifacts/FuoEvolve-macos-${{ matrix.arch }}.dmg"

      - name: Upload macOS DMG
        uses: actions/upload-artifact@v7
        with:
          name: FuoEvolve-macos-${{ matrix.arch }}-dmg
          path: build/artifacts/FuoEvolve-macos-${{ matrix.arch }}.dmg
          archive: false
          retention-days: 14
          if-no-files-found: error

  linux-appimage:
    name: Linux x64 AppImage
    runs-on: ubuntu-22.04
    timeout-minutes: 100

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Prepare Git refs for versioning
        run: |
          if ! git show-ref --verify --quiet refs/heads/master; then
            git branch master origin/master
          fi

      - name: Set up Nucleus GraalVM toolchain
        uses: NucleusFramework/Nucleus/.github/actions/setup-nucleus@v2.5.15
        with:
          java-version: "17"
          graalvm: "true"
          packaging-tools: "true"
          setup-node: "true"

      - name: Set up Rust 1.98.1
        uses: dtolnay/rust-toolchain@1.98.1

      - name: Cache Rust WebView helper
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            desktopApp/native/web-login/target
          key: nucleus-web-login-linux-x64-rust-1.98.1-${{ hashFiles('desktopApp/native/web-login/Cargo.toml', 'desktopApp/native/web-login/Cargo.lock', 'desktopApp/native/web-login/src/**') }}
          restore-keys: |
            nucleus-web-login-linux-x64-rust-1.98.1-

      - name: Install portable Linux native dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libfuse2 \
            libmpv-dev \
            libsecret-1-dev \
            libwebkit2gtk-4.1-dev \
            glib-networking \
            patchelf \
            pax-utils

      - name: Build Nucleus Native Image AppImage
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          ./gradlew \
            --no-daemon \
            --no-configuration-cache \
            --warning-mode all \
            --stacktrace \
            -PenableNucleusDesktopPoc=true \
            -PnativeMarch=compatibility \
            -Pfuoevolve.nucleus.targetFormat=appimage \
            -Pfuoevolve.nucleus.bundleLinuxRuntime=true \
            :desktopNucleusPoc:packageGraalvmAppImage

      - name: Prepare and verify AppImage artifact
        run: |
          mapfile -t artifacts < <(find desktopNucleusPoc/build -type f -iname '*.AppImage' -print)
          if [[ "${#artifacts[@]}" -ne 1 ]]; then
            echo "Expected exactly one AppImage, found ${#artifacts[@]}" >&2
            printf '%s\n' "${artifacts[@]}" >&2
            exit 1
          fi
          mkdir -p build/artifacts build/appimage-verify
          cp "${artifacts[0]}" build/artifacts/FuoEvolve-linux-x64.AppImage
          chmod +x build/artifacts/FuoEvolve-linux-x64.AppImage
          (
            cd build/appimage-verify
            ../artifacts/FuoEvolve-linux-x64.AppImage --appimage-extract >/dev/null
            ROOT=squashfs-root
            find "$ROOT" -type f -name libfuoevolve_mpv_jni.so -print -quit | grep -q .
            find "$ROOT" -type f -name 'libmpv.so*' -print -quit | grep -q .
            find "$ROOT" -type f -name fuoevolve-web-login -print -quit | grep -q .
            find "$ROOT" -type f -name WebKitNetworkProcess -print -quit | grep -q .
            find "$ROOT" -type f -name libgiognutls.so -print -quit | grep -q .
          )
          du -h build/artifacts/FuoEvolve-linux-x64.AppImage

      - name: Upload Linux AppImage
        uses: actions/upload-artifact@v7
        with:
          name: FuoEvolve-linux-x64-appimage
          path: build/artifacts/FuoEvolve-linux-x64.AppImage
          archive: false
          retention-days: 14
          if-no-files-found: error

  linux-arch:
    name: Linux x64 Arch package
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Prepare Git refs for versioning
        run: |
          if ! git show-ref --verify --quiet refs/heads/master; then
            git branch master origin/master
          fi

      - name: Set up Nucleus GraalVM toolchain
        uses: NucleusFramework/Nucleus/.github/actions/setup-nucleus@v2.5.15
        with:
          java-version: "17"
          graalvm: "true"
          packaging-tools: "true"
          setup-node: "true"

      - name: Set up Rust 1.98.1
        uses: dtolnay/rust-toolchain@1.98.1

      - name: Cache Rust WebView helper
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            desktopApp/native/web-login/target
          key: nucleus-web-login-linux-x64-rust-1.98.1-${{ hashFiles('desktopApp/native/web-login/Cargo.toml', 'desktopApp/native/web-login/Cargo.lock', 'desktopApp/native/web-login/src/**') }}
          restore-keys: |
            nucleus-web-login-linux-x64-rust-1.98.1-

      - name: Install Linux native dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            libmpv-dev \
            libsecret-1-dev \
            libwebkit2gtk-4.1-dev \
            libarchive-tools

      - name: Build Nucleus Native Image Arch package
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          ./gradlew \
            --no-daemon \
            --no-configuration-cache \
            --warning-mode all \
            --stacktrace \
            -PenableNucleusDesktopPoc=true \
            -PnativeMarch=compatibility \
            -Pfuoevolve.nucleus.targetFormat=pacman \
            :desktopNucleusPoc:packageGraalvmPacman

      - name: Prepare and verify Arch artifact
        run: |
          mapfile -t artifacts < <(find desktopNucleusPoc/build -type f \( -name '*.pacman' -o -name '*.pkg.tar.zst' \) -print)
          if [[ "${#artifacts[@]}" -ne 1 ]]; then
            echo "Expected exactly one Arch/pacman package, found ${#artifacts[@]}" >&2
            printf '%s\n' "${artifacts[@]}" >&2
            exit 1
          fi
          package="${artifacts[0]}"
          bsdtar -xOf "$package" .PKGINFO | tee build/arch-pkginfo.txt
          grep -Eq '^depend = mpv($|[<>=])' build/arch-pkginfo.txt
          grep -Eq '^depend = libsecret($|[<>=])' build/arch-pkginfo.txt
          grep -Eq '^depend = webkit2gtk-4.1($|[<>=])' build/arch-pkginfo.txt
          bsdtar -tf "$package" | grep -E 'libfuoevolve_mpv_jni\.(so|dll|dylib)$' >/dev/null
          bsdtar -tf "$package" | grep 'fuoevolve-web-login' >/dev/null
          mkdir -p build/artifacts
          cp "$package" build/artifacts/FuoEvolve-linux-x64.pkg.tar.zst
          du -h build/artifacts/FuoEvolve-linux-x64.pkg.tar.zst

      - name: Upload Arch Linux package
        uses: actions/upload-artifact@v7
        with:
          name: FuoEvolve-linux-x64-arch
          path: build/artifacts/FuoEvolve-linux-x64.pkg.tar.zst
          archive: false
          retention-days: 14
          if-no-files-found: error
